---
import { Icon } from 'astro-icon/components';

import type { SocialButtonProps } from '../../data/socials';

const props = Astro.props as SocialButtonProps;
---

<a
	href={props.href}
	aria-label={props.name}
	target="_blank"
	class="group flex aspect-square size-10 items-center justify-center rounded-full border border-gray-200 bg-white transition-all hover:scale-110 hover:border-ghall-green/20 hover:shadow-sm"
>
	<Icon
		class="text-2xl transition-colors group-hover:text-ghall-green"
		name={props.icon}
	/>
</a>
